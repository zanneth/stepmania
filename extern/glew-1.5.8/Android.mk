LOC_GLEW_PATH:= $(call my-dir)
LOCAL_PATH := $(LOC_GLEW_PATH)

include $(CLEAR_VARS)

LOCAL_MODULE:= glew
LOCAL_SRC_FILES := src/glew.c
LOCAL_CFLAGS := -DGLEW_NO_GLU
LOCAL_C_INCLUDES := $(LOC_GLEW_PATH)/include
LOCAL_EXPORT_C_INCLUDES := $(LOC_GLEW_PATH)/include

include $(BUILD_STATIC_LIBRARY)
